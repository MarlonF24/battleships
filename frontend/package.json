{
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.3.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "generate:api": "openapi-generator-cli generate -i http://localhost:8000/openapi.json -g typescript-fetch -o src/api-client"
  },
  "type": "module",
  "dependencies": {
    "navigo": "^8.11.1"
  }
}
